/* -------------------------------------- Adapt below part ----------------------------------- */

const component = 'member/events/Events.vue'

// needs translation
const page = 'Activiteiten'
const title = page

/* -------------------------------------- Adapt above part ----------------------------------- */

import { load } from '@/template/default.js'
load(component, page, title, true)